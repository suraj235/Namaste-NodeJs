// Modules protects the variables and functions from being accessed outside this file.

function nightGreet(name) {
  console.log('Hello, ' + name + '! Good Night. Sweet Dreams!');
}

module.exports = nightGreet; 